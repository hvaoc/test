# Copy to .env to enable real frontier-LLM responses. Without it, Cairn runs on a
# deterministic mock so the app is always demoable (no key required).
#
# The key is read ONLY by the server API routes (app/api/*+api.ts) — it is never
# bundled into the mobile client.
ANTHROPIC_API_KEY=
